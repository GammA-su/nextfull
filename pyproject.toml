[project]
name = "sentcodelm"
version = "0.1.0"
requires-python = ">=3.10,<3.11"
dependencies = [
  "torch",
  "numpy<2",
  "tqdm",
  "regex",
  "xxhash",
  "datasets",
  "faiss-gpu",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["tools"]
py-modules = ["utils"]
